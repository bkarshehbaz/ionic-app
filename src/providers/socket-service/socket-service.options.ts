export type SocketCallback = (data: any) => void;

// export interface SocketClient extends SocketIOClient.Socket {
//     /**
//      * Called upon a server event.
//      *
//      * @param {Object} packet
//      * @api private
//      */
//     onevent: (packet: { data: Array<{ propertyID: number, userID: number}> }) => void;
// }
