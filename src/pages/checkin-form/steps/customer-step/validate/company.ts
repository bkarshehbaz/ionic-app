// import { CustomerStep } from "../customer-step";
// import { invalid, valid } from "../../../../../constants/constants";
// import { isEmpty, isNumber } from "lodash";

// export function companyEvent($invalid = invalid): boolean {

// 	let me: CustomerStep = this;

// 	const { companyEventName, companyEventID } = me.ticket;

// 	me.validations.companyEvent = !isEmpty(companyEventName) && isNumber(companyEventID) ? valid : $invalid;

// 	return true;
// }
