// import { CustomerStep } from "../customer-step";
// import { valid, invalid } from "../../../../../constants/constants";
// import { toString } from "lodash";

// export function roomNumber() {

// 	let me: CustomerStep = this;

// 	const { roomNumber } = me.ticket;

// 	const len = toString(roomNumber).trim().length;

// 	me.validations.roomNumber = len === 3 || len === 4 ? valid : invalid;

// 	return me.validations.customerPhone === valid ? true : false;
// }
