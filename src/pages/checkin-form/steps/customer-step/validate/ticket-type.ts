// import { CustomerStep } from "../customer-step";
// import { invalid, valid } from "../../../../../constants/constants";
// import { isEmpty, isNumber } from "lodash";

// export function ticketType(): boolean {

// 	let me: CustomerStep = this;
// 	const { ticketTypeID, ticketTypeName } = me.ticket;

// 	me.validations.ticketType = !isEmpty(ticketTypeName) && isNumber(ticketTypeID) ? valid : invalid;
// 							//   ? ( highlight ? valid : "validNoHighlight" )
// 							//   : invalid;

// 	return me.validations.ticketType === valid ? true : false;

// }
