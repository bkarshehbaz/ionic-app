// import { IUser } from "../lib/vvs-bridge";
// import { getFullName } from "./index";

// export const getUserInitials = (user = {} as IUser): string  => {
//     let initials: string[]|string = getFullName(user).match(/\b\w/g) || [];
//     initials = ( initials.shift() || "") + (initials.pop() || "").toUpperCase();
//     return initials;
// };
