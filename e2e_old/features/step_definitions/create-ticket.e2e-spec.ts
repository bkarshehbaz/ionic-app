import { CallbackStepDefinition, Given, StepDefinitions, Then } from "cucumber";
import { companySpec } from "../../e2e-classes/login/select-company/company.e2e-class";

Given('This is our first test', () => {
    // companySpec.run();
});

Then('it should pass', () => {

});
